import { CamelCaseToSpacePipe } from './camel-case-to-space.pipe';

describe('CamelCaseToSpacePipe', () => {
  it('create an instance', () => {
    const pipe = new CamelCaseToSpacePipe();
    expect(pipe).toBeTruthy();
  });
});
