import { MinTwoDigitsPipePipe } from './min-two-digits-pipe.pipe';

describe('MinTwoDigitsPipePipe', () => {
  it('create an instance', () => {
    const pipe = new MinTwoDigitsPipePipe();
    expect(pipe).toBeTruthy();
  });
});
