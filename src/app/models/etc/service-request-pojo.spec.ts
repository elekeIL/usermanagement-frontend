import { ServiceRequestPojo } from './service-request-pojo';

describe('ServiceRequestPojo', () => {
  it('should create an instance', () => {
    expect(new ServiceRequestPojo()).toBeTruthy();
  });
});
